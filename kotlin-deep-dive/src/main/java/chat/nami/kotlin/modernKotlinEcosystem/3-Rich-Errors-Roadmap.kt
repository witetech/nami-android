package chat.nami.kotlin.modernKotlinEcosystem

// Roadmap awareness: Rich Errors / Union Types for Errors — no syntax to run yet.
//
// Direction announced (KotlinConf 2025): error unions like `User | NetworkError` —
// constrained unions (errors only) to make Result/Either patterns first-class with
// compiler-enforced handling.
//
// Status: design/roadmap — teach as "where the language is going", not as available syntax.
